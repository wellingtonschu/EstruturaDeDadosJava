package Lista01;

import java.util.ArrayList;
import java.util.List;

public class Ex02 {/*

    protected List<Pessoa> pessoa;

    public Ex02(){
        pessoa = new ArrayList<Pessoa>();
    }

    public void incluirPessoa(String nome, String endereco, String telefone){
        Pessoa pes = new Pessoa();
        pes.setNome(nome);
        pes.setEndereco(endereco);
        pes.setTelefone(telefone);
        pessoa.add(pes);
    }

    public String consultarPessoa(String nome){
        for (int x = 0; x < pessoa.size(); x++){
            if (pessoa.get(x).getNome() == nome){
                String texto = " Nome: " + pessoa.get(x).getNome() + "; Endereço: " + pessoa.get(x).getEndereco() + "; Telefone: " + pessoa.get(x).getTelefone();
                return texto;
            }
        }
        return "Pessoa não encontrada";
    }
    public void excluirPessoa(String nome){
        for (int x = 0; x < pessoa.size(); x++){
            if (pessoa.get(x).getNome() == nome){
                pessoa.remove(x);
            }
        }
    }
*/}
